import Header from '../../Header';

const AboutHeader: React.FC = () => {
  return (
    <Header
      title="About Mind"
      subheader="Learn more about the site"
    />
  );
};

export default AboutHeader;
