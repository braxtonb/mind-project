import Header from '../../Header';

const NewHeader: React.FC = () => {
  return (
    <Header
      title="Add an inspiration"
      subheader="Anything can be inspirational"
    />
  );
};

export default NewHeader;
