version: '3'

services:
  ui:
    build:
      context: ./mind
    ports:
      - 3000:80

  json-server:
    build:
      context: ./mind-json-server
    command: npm start
    ports:
      - 4000:4000